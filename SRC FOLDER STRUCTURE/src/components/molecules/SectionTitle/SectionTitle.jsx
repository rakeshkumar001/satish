import React from 'react'

const SectionTitle = ({ title, descp }) => {
    return (
        <div>
            <h3>{title}</h3>
            <p>{descp}</p>
        </div>
    )
}
export default SectionTitle